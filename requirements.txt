# Core dependencies for the Cloud-Native Reliability Engineering Platform.
# Vulnerability status is evaluated by the CI Trivy gates; do not treat comments
# here as security evidence.

# Web framework
starlette==1.3.1
fastapi==0.141.1
uvicorn[standard]==0.32.1
pydantic==2.10.5
pydantic-core==2.27.2

# Redis is used for the API tenant rate limiter. Payments intentionally uses
# in-memory storage in this small demonstration workload.
redis==5.0.1

# HTTP client
httpx==0.25.1
requests==2.33.0

# Monitoring
prometheus-client==0.19.0
prometheus-fastapi-instrumentator==8.1.0

# Testing
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==6.0.0

# Linting/formatting (optional, for local dev)
ruff==0.1.6

# Security pins for transitive/build tooling vulnerabilities flagged in images
jaraco.context>=6.1.0
wheel>=0.46.2
