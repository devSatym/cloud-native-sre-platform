# Development/test dependencies intentionally reuse the runtime dependency set.
# The application imports Redis and Prometheus instrumentation during test
# collection, so a separate lighter list had made `make install && make test`
# unreliable.
-r requirements.txt
